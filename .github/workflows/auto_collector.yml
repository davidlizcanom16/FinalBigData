name: Auto Data Collector

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  collect-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    
    - name: Install dependencies
      run: |
        pip install requests pymongo python-dotenv
    
    - name: Create .env file
      env:
        COSMOS_CONNECTION_STRING: ${{ secrets.COSMOS_CONNECTION_STRING }}
      run: |
        echo "COSMOS_CONNECTION_STRING=${COSMOS_CONNECTION_STRING}" > .env
    
    - name: Run data collector
      run: |
        python github_collector.py
```

4. **"Commit changes..."**
5. Message: `Simplify workflow - use separate script`
6. **"Commit changes"**

---

## ✅ Ahora debería funcionar

1. Ve a **Actions**
2. Espera unos segundos y deberías ver el workflow ejecutándose
3. Debería mostrar **check verde ✅**

---

## 🧪 Probar manualmente

1. En **Actions** → **Auto Data Collector**
2. Click **"Run workflow"** (ahora debería aparecer)
3. **"Run workflow"** (botón verde)
4. Espera 1 minuto
5. Haz clic en el workflow que se está ejecutando
6. Click en "collect-data" para ver los logs

Deberías ver:
```
✅ Conectado a Azure Cosmos DB
📦 Recolectados 20 productos
✅ 20 productos insertados en Azure
🎉 Recolección completada exitosamente
